/**
 * Fast visible lane: read-only tool turn, same-chat text attachment, durable
 * reload proof, and marker-owned recoverable cleanup.
 */
import { makeChatLiveAttachmentRoundTrip } from "./chatview-live-acceptance.mjs";

export default makeChatLiveAttachmentRoundTrip;
