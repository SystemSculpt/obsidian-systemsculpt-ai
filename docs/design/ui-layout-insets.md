# UI layout insets

The overlap-based inset helper was removed during the managed-only UI cleanup.
No active production view currently depends on a runtime inset manager or a
status-bar offset CSS contract.
