import { Notice } from "obsidian";
import { AnkiModule } from "../AnkiModule";

export async function createAnkiCard(plugin: AnkiModule): Promise<void> {
  new Notice("Creating Anki card... (coming soon)");
}
